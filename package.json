{
  "name": "unseen-odyssey-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "generate:codes": "node scripts/generate-codes.mjs"
  }
}
